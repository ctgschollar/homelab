---
- name: Deploy edge node services
  hosts: edge_nodes
  become: true

  vars:
    required_tools: [cloudflared]

  pre_tasks:
    - name: Install required tools
      import_role:
        name: tools

  roles:
    - cloudflared
    - traefik-edge
