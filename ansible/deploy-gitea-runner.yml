---
- name: Deploy Gitea act runner
  hosts: gitea_runners
  become: true

  vars:
    required_tools: [docker]

  pre_tasks:
    - name: Install required tools
      import_role:
        name: tools

  roles:
    - gitea-runner
