---
- name: Deploy Gitea act runner
  hosts: gt-runner-01 
  become: true

  vars:
    required_tools: [docker, node, make, hatch, git]

  pre_tasks:
    - name: Install required tools
      import_role:
        name: tools

  roles:
    - gitea-runner
    - promtail
